# =============================================================================
#  File: deploy/cloud/ansible/inventory.ini
#  Purpose: hosts for the personal-cloud strong tier. `gpu_box` is the colo
#           GPU server; `pi` is the home Raspberry Pi running the orchestrator.
#           Replace the addresses with your own (the gpu_box address is reached
#           for provisioning; the Pi serves the orchestrator).
#  History
#  Date         Author          Changes
#  2026-06-19   Anubhav Sigdel  initial — gpu_box + pi groups
# =============================================================================

[gpu_box]
colo-gpu ansible_host=203.0.113.10 ansible_user=root

[pi]
home-pi ansible_host=10.10.0.2 ansible_user=pi
